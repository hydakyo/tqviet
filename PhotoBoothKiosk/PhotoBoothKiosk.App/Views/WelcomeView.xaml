<UserControl x:Class="PhotoBoothKiosk.App.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <!-- Toàn màn hình: click/chạm ở bất kỳ đâu để Start -->
    <Button Command="{Binding StartCommand}"
            Background="Transparent"
            BorderThickness="0"
            Focusable="False">
      <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
          <TextBlock Style="{StaticResource H1}"
                     Text="Chạm để bắt đầu"
                     HorizontalAlignment="Center"/>

          <!-- “Touch to Start” nhấp nháy -->
          <TextBlock Text="Touch to Start"
                     FontSize="28"
                     HorizontalAlignment="Center"
                     Opacity="1">
            <TextBlock.Triggers>
              <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                  <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="1" To="0.25" Duration="0:0:0.9"/>
                  </Storyboard>
                </BeginStoryboard>
              </EventTrigger>
            </TextBlock.Triggers>
          </TextBlock>

          <!-- Nút lớn (dành cho người dùng không chạm vùng trống) -->
          <Button Style="{StaticResource KioskButton}"
                  Content="Bắt đầu"
                  Command="{Binding StartCommand}"
                  Width="320"
                  HorizontalAlignment="Center"/>
        </StackPanel>
      </Grid>
    </Button>
  </Grid>
</UserControl>
